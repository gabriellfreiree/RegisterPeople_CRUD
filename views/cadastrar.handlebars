<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="/stylesheet/output.css" rel="stylesheet">
    <title>Document</title>
</head>
<body class="bg-black1 text-white">
 <header class="bg-orange  text-center py-4 flex justify-center items-center">
        <img src="/img/LogoNav.png" alt="Logo e marketing do sistema" class="w-24 h-24 mr-4">
        <nav class="flex justify-center space-x-4 mt-4">
            <a href="/" class="hover:underline">Home</a>
            <a href="/Cadastrar" class="hover:underline">Cadastrar</a>
            <a href="/Listar" class="hover:underline">Listar</a>
            <a href="/QuemSomos" class="hover:underline">Quem Somos</a>
            <a href="/Deslogar" class="hover:underline">Deslogar</a>
        </nav>
    </header>

    <main class="container mx-auto py-8 text-white">
        <div class="max-w-md mx-auto bg-white p-6 rounded-lg shadow-md bg-orange">
            <h1 class="text-2xl font-bold mb-6">Cadastro Pessoal</h1>
            <form class="bg-range">
            <div class="mb-4">
                <div class="mb-4">
                {{!-- nome --}}
                <label for="nome" class="block text-white">Nome</label>
                <input type="text" id="nome" class="w-96 px-3 py-1 border rounded-md bg-white"><br>
                </div>
                {{!-- idade --}}
                <div class="mb-4">
                <label for="idade" class="block text-white">Idade</label>
                <input type="text" id="idade" class="w-64 px-3 py-1 border rounded-md bg-white"><br>
                </div>
                {{!-- sexo --}}
                <div class="mb-4">
                <label for="sexo" class="block text-white">Sexo</label>
                <select name="sexo" class="w-40 px-3 py-1 border rounded-md bg-white" required>
                <option value="">Nenhum</option>
                <option value="masculino">Masculino</option>
                <option value="feminino">Feminino</option>
                </select>
                </div>
                {{!-- cep --}}
                <div class="mb-4">
                <label for="cep" class="block text-white">CEP</label>
                <input type="text" id="cep" class="w-64 px-3 py-1 border rounded-md bg-white"><br>
                </div>
                {{!-- endereco --}}
                <div class="mb-4">
                <label for="endereco" class="block text-white">Endereço</label>
                <input type="text" id="endereco" class="w-64 px-3 py-1 border rounded-md bg-white"><br>
                </div>
            </div>
            <button type="submit" class="bg-colorbutton text-white font-bold py-1 px-4 rounded shadow-inner shadow-md">Cadastrar</button>
            </form>
        </div>
    </main>

    <footer class="bg-orange text-white text-center py-4">
        <p class="text-white">®Desenvolvido para Estudo</p>
    </footer>
</body>
</html>